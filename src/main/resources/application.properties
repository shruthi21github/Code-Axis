spring.application.name=api 

app.database.name=code_axis_db_dev
spring.datasource.url=jdbc:mysql://localhost:3306/${app.database.name}
spring.datasource.username=root
spring.datasource.password=fusion5Tech@

# 1. Primary: Disables Hibernate's DDL (create, update, drop)
spring.jpa.hibernate.ddl-auto=none

# 2. Secondary: Disables Spring's internal DDL generation logic
spring.jpa.generate-ddl=false

# 3. Extra Safety: Disables automatic bean validation at the JPA level
# This prevents errors if your Java fields don't perfectly match your DB constraints
spring.jpa.properties.jakarta.persistence.validation.mode=none

# Logging
spring.jpa.show-sql=true

spring.web.error.include-message=always